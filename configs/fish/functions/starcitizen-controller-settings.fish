function starcitizen-controller-settings --description "Print Star Citizen controller guidance"
    echo "Old command stopped working; run `rsi-launcher --shell`"
    echo " "
    echo "and then run `wine control joy.cpl` for now."
end
