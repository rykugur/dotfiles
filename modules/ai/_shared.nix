{
  # Bash commands allowed in both claude-code and opencode
  allowedBashCommands = [
    "curl"
    "gh api"
    "helm template"
    "kubectl get"
    "kubectl logs"
    "nix search"
    "git"
    "ls"
    "find"
  ];
}
